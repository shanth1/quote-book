const H2 = ({ children }) => {
    return (
        <h2 className="text-lg font-bold leading-tight tracking-tight text-gray-900  dark:text-white">
            {children}
        </h2>
    );
};

export default H2;
